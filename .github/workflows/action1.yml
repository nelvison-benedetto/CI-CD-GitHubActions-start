name: First Workflow
on: workflow_dispatch    #event can be trigged manually from e.g.CLI
jobs:
  first-job:
    runs-on: windows-latest
    steps:
      - name: Print greeting
        run: echo "hello world"
      - name: Print goodbye
        run: echo "done - bye!"
